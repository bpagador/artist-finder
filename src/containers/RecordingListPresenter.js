import React from 'react';
import PropTypes from 'prop-types';

const RecordingListPresenter = props => {
  return (
    <div>
            hi!
    </div>
  );
};

RecordingListPresenter.propTypes = {
    
};

export default RecordingListPresenter;
